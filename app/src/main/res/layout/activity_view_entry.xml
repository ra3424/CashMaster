<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:background="@color/background"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.rahul.indrakant.cashmaster.ViewEntry">
    <TextView
        android:textColor="#3462D6"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Select Date"
        android:textSize="@dimen/font"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:id="@+id/text_date"
        />

    <EditText
        android:layout_toRightOf="@id/text_date"
        android:textColor="#3462D6"
        android:editable="false"
        android:textSize="@dimen/font"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Date"
        android:id="@+id/viewEntry_datePicker"
        android:focusable="false"
       android:layout_marginLeft="@dimen/activity_horizontal_margin"
        android:layout_marginTop="5dp"
        />
    <TextView
        android:textColor="@color/red"
        android:layout_below="@id/viewEntry_datePicker"
        android:textSize="@dimen/font"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/viewEntry_total"
        android:text="Expenses: Rs "
        />

    <ListView
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:id="@+id/viewEntry_listView"
        android:layout_below="@+id/viewEntry_total"/>

</RelativeLayout>
